#pragma once // para que no se duplique
#include <iostream>
#include <string>
#include <cstdio>

class Color
{
private:

public:
    Color(/* args */) {}
    ~Color() {}
    void Colorear()
    {
    }
};